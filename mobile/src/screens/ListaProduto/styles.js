import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {},
  icon: {
    paddingHorizontal:10, 
    paddingVertical:0
  },
  textCardSize: {
    fontSize: 16,
  },
  textCardBold: {
    fontSize: 18,
    fontWeight: '700'
  }
});